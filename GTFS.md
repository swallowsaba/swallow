# 京王バス・小田急バス・西東京バス・関東バスを使えるようにする(GTFS の取り込み)

`tools/build-gtfs-index.mjs` / `.github/workflows/gtfs.yml` / `transit/js/gtfs.js`

---

## あなたがやること(1 回だけ・5 分)

手元の PC には何もインストールしません。**ブラウザだけ**で終わります。

### 1. ファイルをアップロードする

リポジトリに次を追加してください。

```
.github/workflows/gtfs.yml     ← 新規
tools/build-gtfs-index.mjs     ← 新規
tools/gtfs-lib.mjs             ← 新規
tools/gtfs-sources.json        ← 新規(取り込む事業者の一覧)
transit/js/gtfs.js             ← 新規
transit/ の残り                 ← 上書き
```

### 2. ODPT のトークンを GitHub に登録する

GitHub のリポジトリで

**Settings → Secrets and variables → Actions → New repository secret**

| 項目 | 値 |
|---|---|
| Name | `ODPT_TOKEN` |
| Secret | ODPT のアクセストークン |

> **前後に空白を入れないでください。** 以前と同じ落とし穴です。

### 3. 1 回だけ手で動かす

**Actions → 「GTFS 取り込み」→ Run workflow**

3〜10 分で終わります。成功すると `transit/data/gtfs/` にファイルが追加され、
既存の deploy ワークフローが自動で走って公開されます。

### 4. 確認する

アプリの「対応範囲」ボタンを押すと、**対応している事業者**の側に

```
京王バス(バス)
  GTFS を取り込み済み(2026-09-10 時点のダイヤ)
```

と出ていれば成功です。

---

## 事業者を足す

**`tools/gtfs-sources.json` を編集するだけ**です。スクリプトは触りません。

取り込み元は 2 種類書けます。

```jsonc
// 1) ODPT のデータセット(トークンが要る。URL はカタログから毎回引き直す)
{
  "id": "KeioBus",
  "title": "京王バス",
  "kind": "odpt",
  "dataset": "keio_bus_all_lines",
  "license": "公共交通オープンデータ基本ライセンス",
  "attribution": "公共交通オープンデータセンター"
}

// 2) GTFS(ZIP)の URL を直接指定(GTFS データリポジトリ・事業者の自社サイトなど)
{
  "id": "KeikyuBus",
  "title": "京浜急行バス",
  "kind": "url",
  "url": "https://example.jp/gtfs/keikyu.zip",
  "license": "CC BY 4.0",
  "attribution": "京浜急行バス"
}
```

`url` 指定の場合は **中身のハッシュ**で変更を判定します(URL が変わらないまま
ダイヤ改正されることがあるため)。ODPT 指定の場合はリソース ID で判定するので、
ダウンロードする前に「変わっていない」と分かります。

`attribution` に書いた名前は、**画面のフッタに出典として表示**されます。

### 足す前に必ず確認すること

ここで取り込んだデータは GitHub Pages に置かれます。これは**再配布**にあたります。

| ライセンス | 可否 |
|---|---|
| CC0 / CC BY / CC BY-SA | ○(CC BY 系は `attribution` を必ず書く) |
| 政府標準利用規約 | ○(出典表示) |
| 公共交通オープンデータ基本ライセンス | ○ **ただし出典表示が必須**(2026-09 に規約を確認済み) |
| **チャレンジ限定ライセンス** | **×**(コンテスト目的外の利用が禁止) |
| **再配布禁止・個人利用限定** | **×** |

**規約を読まずに足さないでください。**

### 出典表示は義務です

基本ライセンスは再配布を認めていますが、**出典表示が条件**です。そのため
`attribution` を書き忘れたまま公開すると規約違反になります。

アプリ側は次のようにしています。

- フッタに「バスの時刻表データ出典: ○○(事業者名) — ライセンス名」を**常時表示**
- Worker が落ちていても表示する(索引を配信している以上、出典は要るため)
- スモークテストで「隠れていないこと・事業者名まで出ていること」を検証

`attribution` を空にすると出典が出ないので、**必ず書いてください**。

### 何が設定されているか見る

```bash
node tools/build-gtfs-index.mjs --list
```

GitHub 上では **Actions → 「GTFS 取り込み」→ Run workflow** で
「一覧を見るだけ」にチェックを入れると、取り込まずに一覧だけ出せます。

---

## そのあとは自動

毎月末に自動で実行されます。**元データが前回と同じときは何もコミットしません**ので、
無駄なデプロイもリポジトリの肥大化も起きません。

バスのダイヤ改正は年に数回なので、実際にコミットが起きるのも年に数回です。

### deploy も自動で起こします

GitHub には「ワークフローが `GITHUB_TOKEN` で push したコミットは、他のワークフローを
起動しない」という仕様があります(無限ループ防止。設定では変えられません)。

そのままだと索引を取り込んでも**公開されない**ので、取り込みの最後に deploy を
明示的に呼んでいます。`workflow_dispatch` は例外として `GITHUB_TOKEN` でも起動できます。

```yaml
permissions:
  contents: write
  actions: write          # deploy を呼ぶために必要

- name: 公開(deploy)を起こす
  if: steps.commit.outputs.changed == 'yes'
  run: gh workflow run deploy.yml --ref main
```

変更が無かった月は呼びません。

---

## 費用

**すべて無料枠の内側です。**

| | 使う量 | 無料枠 |
|---|---|---|
| GitHub Actions | 月 1 回 × 約 5 分 | Public リポジトリは無制限 / Private でも月 2000 分 |
| GitHub Pages | 索引のファイル(数 MB) | 1 GB まで |
| Cloudflare Workers | **使いません**(静的ファイルを直接読むため) | — |
| ODPT | ダウンロード 月 3 回 | 無償 |

Worker を通さないので、**バス検索のぶん Worker の無料枠が空きます。**

---

## 仕組み

### なぜ索引を作るのか

GTFS は「1 事業者の全時刻表」が入った ZIP です。展開すると数十 MB になり、

- ブラウザに丸ごと渡すことはできない
- Cloudflare Workers の無料枠(CPU 10ms / メモリ 128MB)では展開すらできない

ので、そのままでは使えません。そこで GitHub Actions 側で小さく割っておきます。

```
GTFS(ZIP 数十MB)
   ↓  GitHub Actions(月1回)
transit/data/gtfs/<事業者>/
   index.json          … 停留所の名前・座標・通る系統   (1 ファイル)
   patterns/0.json …   … 系統ごとの時刻表             (40 系統ずつ)
   ↓  ブラウザ
索引を 1 回読む → 必要な系統のファイルだけ取りに行く
```

検索のときにブラウザが読むのは、**索引 1 つと、実際に関係する系統ファイルだけ**です。

### 停車パターンでまとめている

GTFS は 1 便ごとに全停留所の時刻を持つので、同じ経路を何百回も書くことになります。
停車順が同じ便はひとつにまとめ、時刻だけを配列で持つようにしてファイルを小さくしています。

### ダウンロード URL は決め打ちしない

事業者ごとにファイル名が違います(京王は `AllLines.zip`、小田急は `AIILines.zip`)。
決め打ちすると壊れるので、ODPT のデータカタログ(CKAN)から
**その日に有効なダイヤのリソース**を毎回引き直しています。

---

## 制約(必ず読んでください)

### これはスナップショットです

取り込んだ時点のダイヤです。**取り込み後のダイヤ改正・臨時運行・運休は反映されません。**

そのため、この事業者のバスが経路に出たときは必ず

> 京王バス は取り込み済みのダイヤで計算しています
> 京王バス: 2026-09-10 取り込み

と画面に出します。ODPT の API から都度取っている都営バス等とは性質が違うためです。

### 運行情報(遅延・運休)はありません

GTFS は時刻表だけです。リアルタイム情報(GTFS-RT)は別データで、今は使っていません。

### 乗り継ぎには使っていません

今のところ**直通便のみ**です。GTFS 由来のバスと鉄道を乗り継ぐ経路は出しません
(停留所と駅の対応づけが必要で、まだ入れていません)。

### 運行日の定義が無い便は出しません

`calendar.txt` に定義が無い `service_id` の便は、**走らせません**。
「たぶん毎日走る」と決めつけて存在しない便を出すよりましだからです。
その場合は取り込みのログに警告が出ます。

---

## うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| `ODPT_TOKEN が未設定です` | Secret の名前が違う。`ODPT_TOKEN` と正確に |
| `ODPT_TOKEN が拒否されました` | トークンが誤っているか、前後に空白がある |
| `ZIP ではないものが返りました` | トークンが無効。エラー本文がログに出ます |
| `データカタログを読めませんでした` | ODPT 側の一時的な障害。時間をおいて再実行 |
| 1 事業者だけ失敗 | 他は取り込まれています。ログの「まとめ」を確認 |
| 「対応範囲」に出ない | ワークフローが成功しても deploy が終わるまで数分かかります |

失敗しても**既存の索引は消えません**。前の状態のまま動き続けます。

---

## 手元で試したいとき(任意)

Node があれば動きます。無くても上の手順だけで完結するので、必須ではありません。

```bash
ODPT_TOKEN=xxxxx node tools/build-gtfs-index.mjs            # 全事業者
ODPT_TOKEN=xxxxx node tools/build-gtfs-index.mjs KeioBus    # 1 事業者だけ
node tools/build-gtfs-index.mjs --list                      # 設定の確認だけ(トークン不要)
```

---

## 検証

```bash
node tests/gtfs.test.mjs     # 57 件
node tests/ui.smoke.mjs      # 画面(取り込み済みバスの表示・取り込み日の明示)
```

確認している内容(抜粋):

- 引用符・改行・BOM・CRLF を含む GTFS の CSV を壊さず読む
- 24 時を超える時刻(25:05)を営業日基準の分に直す
- のりば群(`location_type=1`)を停留所と取り違えない
- 停車順が同じ便をまとめ、時刻順に並べる
- 曜日と `calendar_dates` の例外(祝日の運休・臨時運行)を正しく判定する
- 有効期間の外の便を出さない
- **運行日の定義が無い便を走らせない**
- 必要なファイルが欠けていたら止める(空の索引を作らない)
- 出発時刻より前の便・逆方向の便を出さない
- 停留所が見つからないときは系統ファイルを取りに行かない(無駄な通信をしない)
- 索引が無いときは静かに 0 件(まだ取り込んでいない状態として扱う)
- 索引が壊れているときは警告として伝える(黙って 0 件にしない)
- 取り込み元の設定(`gtfs-sources.json`)に不備があれば、取り込む前にまとめて指摘する
- 地図に出す停留所が上限に達しても、**どの事業者のものも必ず出る**(先頭の事業者で
  枠を使い切らない)。上限に達していなければ全件出す
