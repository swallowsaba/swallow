{
  "name": "kanto-transit-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ODPT proxy Worker for the Kanto transit route planner",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
