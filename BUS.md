# バス機能について

## 対応事業者

| 事業者 | ライセンス | 状態 | バス停時刻表 |
|---|---|---|---|
| 東京都交通局(都営バス) | CC BY 4.0 | **今すぐ使える**(トークン不要) | あり |
| 東急バス | 基本ライセンス | **今すぐ使える**(既存トークン) | あり |
| 西武バス | 基本ライセンス | **今すぐ使える**(既存トークン) | 無し* |
| 相鉄バス | 基本ライセンス | **今すぐ使える**(既存トークン) | 無し* |
| 横浜市交通局(市営バス) | 基本ライセンス | **今すぐ使える**(既存トークン) | 無し* |
| 神奈川中央交通(かなちゅう) | チャレンジ2026限定 | 2026-10-01 以降 | 無し* |

\* バス停時刻表(`odpt:BusstopPoleTimetable`)を出していない事業者は、
便別時刻表(`odpt:BusTimetable`)の停車時刻から発車時刻を組み立てています。
利用者から見た動作は同じです。

国際興業・東武バスもチャレンジ限定ライセンスで提供されています。
必要であれば `worker/src/bus.js` の `CHALLENGE_BUS_OPERATORS` に追記できます。

**トークンが無い場合は都営バスだけに自動縮退します**(エラーにはなりません)。

---

## できること / できないこと

| | 可否 |
|---|---|
| 乗り換えなしで行ける都営バスの直通便を、実際の発着時刻つきで提示 | **できる** |
| 鉄道の経路と並べて、所要時間・乗換回数で比較 | **できる** |
| **バス → 徒歩 → 鉄道** の複合経路 | **できる** |
| **鉄道 → 徒歩 → バス** の複合経路 | **できる** |
| バス停を出発地・目的地に指定する | **できる**(候補から「バス停として検索」) |
| バス → 鉄道 → バス(バスが 2 本) | できない |
| バス同士の乗り継ぎ | できない |
| 渋滞・遅延の反映 | できない(ODPT にバスの運行情報が無い) |
| 上表以外の事業者 | 未対応(小田急バス・京王バスなどは GTFS 形式のみで JSON API が無い) |

---

## 複合経路(バス⇄鉄道)の仕組み

全バス停を取得できないという制約は変わりませんが、**系統データに停留所名が入っている**ことを
利用すれば、全件データ無しで乗り継ぎ地点を見つけられます。

```
1. 出発バス停を通る系統の「停車順」を取得
2. その中から「◯◯駅前」「◯◯駅東口」のような停留所名を拾う
3. 「駅」より前の部分を取り出して(→「◯◯」)、鉄道の駅名と突き合わせる
4. 一致した駅を乗り継ぎ地点として、そこから先を鉄道で探索する
```

目的地側は同じことを逆向きに行います(鉄道で駅まで行き、そこからバス)。

- バス停と駅の間は**一律 5 分の徒歩**として計算します。実際の距離は考慮していません
  (`transit/data/config.json` の `busStationWalkMinutes` で変更できます)
- 乗り継ぎ駅は片側あたり 2 駅までに絞っています。増やすとリクエスト数が増えるためです
- 名前が一致しない停留所(バス停名に駅名が入っていない場合)は乗り継ぎ地点になりません

## なぜ「バスは 1 本まで」なのか

技術的な妥協ではなく、**データの取得方法の制約**です。

ODPT の API は **1 レスポンス 1000 件が上限で、ページングがありません。**
都営バスの停留所ポールはこれを大きく超えるため、鉄道のように
「全件を取得してグラフを作り、その上で経路探索する」ことができません。
全件 JSON も用意されていますが、これは**全国分**になるため実用になりません。

そこで、次の方法をとっています。

```
1. 出発地の名前でバス停を引く   → その停留所を通る「系統の一覧」が付いてくる
2. 目的地の名前でバス停を引く   → 同上
3. 両者の系統 ID の積集合をとる  ← 通信ゼロで「直通で行けるか」が判る
4. 該当した系統だけ停車順を取得  → 出発が先・目的が後 になっているか確認(逆方向を除外)
5. 出発バス停の時刻表を取得     → その日のダイヤに合う発車時刻
6. その便の全停車時刻を取得     → 目的バス停の到着時刻(推定ではなく実データ)
```

この方式なら **1 検索あたり Worker リクエスト 5 回以内**(複合経路を含めても 10 回程度)に収まります。

バスを 2 本使う経路(バス→鉄道→バス)は、乗り継ぎ地点の組み合わせが掛け算で増え、
リクエスト数が倍以上になるため対象外にしています。

---

## バス停名の扱い

ODPT のバス停検索は**完全一致のみ**で、部分一致に対応していません。
一方バス停の正式名は「渋谷駅前」のように駅名と異なります。

そのため、入力された名前から候補を自動生成して総当たりしています。

| 入力 | 試す名前 |
|---|---|
| `渋谷` | 渋谷 / 渋谷駅前 |
| `渋谷駅` | 渋谷駅 / 渋谷駅前 / 渋谷 |

それでも当たらない名前のバス停は見つかりません。
その場合はバス停の正式名を直接入力してください。

---

## ダイヤの判定

バスのカレンダーは鉄道と違い、`Weekday` / `SaturdayHoliday` ではなく
`odpt.Calendar:Specific.Toei.09-100` のような**事業者固有の識別子**で、
適用日が `2026-09-02` のような日付の配列として列挙されています。

都営バスは 49 件のカレンダーがあり、1 リクエストで取得できるため
24 時間キャッシュ + セッション中の使い回しで済ませています。

「この日に適用されるダイヤを特定できませんでした」と出る場合は、
カレンダーの取得に失敗しているか、その日付がどのカレンダーにも
含まれていない(データ更新前など)ケースです。

---

## 検証

```bash
node tests/bus.test.mjs      # 15 件:直通バスと複合経路のロジック
node tests/worker.test.mjs   # 33 件(うちバス 9 件):エンドポイント
node tests/ui.smoke.mjs      # 画面の動作(直通バス・複合経路・チェックのON/OFF・地図)
```

確認している内容:

- 同じ系統上の区間を実時刻つきで返す
- 逆方向(到着が出発より手前)は候補にしない
- 共通の系統が無ければ 0 件。系統の取得すらしない
- その日に適用されないダイヤの便を拾わない
- 出発時刻より前の便を拾わない
- 便別時刻が取れないときは推定にフォールバックし、画面に「推定」と出す
- 1 検索の Worker リクエストが 5 回以内
- CC BY データにアクセストークンを付けない
- バス停名から駅名を取り出せる(「東京駅丸の内北口」→「東京」)
- バス → 徒歩 → 鉄道 の経路が正しい時刻で組み上がる
- 乗り継ぎ駅から目的地へ鉄道で行けない場合は経路を出さない

---

## 京王バスなど、扱えない事業者

**GTFS を取り込めば対応できます。手順は `GTFS.md` を参照してください。**
以下は「なぜ API 形式では扱えないのか」の説明です。

ODPT は API 形式(`odpt:BusstopPole` / `odpt:BusroutePattern` / `odpt:BusTimetable`)の
提供を事業者ごとに順次終了しており、後継は **GTFS(ZIP ファイル)のみ**になっています。

| 事業者 | API 形式の提供終了 | 後継 |
|---|---|---|
| 京王バス | 2025 年 6 月末 | GTFS |
| 西東京バス | 2025 年 6 月末 | GTFS |
| 小田急バス | 2025 年 9 月末 | GTFS |

### なぜ GTFS を使えないのか

このアプリは**検索のたびに ODPT の API を叩く**作りです。GTFS はそれと相性が悪く、

1. GTFS は数十 MB の ZIP。Cloudflare Workers 無料枠(CPU 10ms / メモリ 128MB)では
   展開して検索することができない
2. 事前に取得してリポジトリに置く方法は、**要件で禁止**されている
   (「GitHub Actions によるデータの事前取得・キャッシュは行わない」)

の 2 点から、現在の構成では扱えません。

黙って欠けさせると「バグでは?」と疑うことになるので、
画面の「対応範囲」に**理由つきで**表示しています。

### 使えるようにする方法(実装済み)

GitHub Actions で月に 1 回 GTFS を取り込み、検索用の索引に変換して
`transit/data/gtfs/` に置く仕組みを入れました。ブラウザはその静的ファイルを
直接読むため、**Worker も無料枠も消費しません**。

導入は Secret に `ODPT_TOKEN` を登録してワークフローを 1 回動かすだけです。
詳細と制約(取り込み時点のスナップショットであること)は **`GTFS.md`** にまとめています。

---

## 今後の拡張

**他のバス事業者を足す**
`worker/src/bus.js` の `BUS_OPERATORS` に追記します。ただし東急バス等は
基本ライセンスまたはチャレンジ限定なので、ホストとトークンの指定が必要です。

チャレンジ2026を有効にすると、次の事業者が追加で使えます(`CHALLENGE_BUS_OPERATORS`)。

| 事業者 | 備考 |
|---|---|
| 神奈川中央交通 | 岡田一本杉などの停留所 |
| 東武バス | |
| 国際興業 | |

**バスを 2 本使う経路に対応する**
乗り継ぎ地点の組み合わせが増えるため、リクエスト数の設計を見直す必要があります。

**乗り継ぎ地点の精度を上げる**
今は停留所名と駅名の一致で判定し、徒歩は一律 5 分としています。
バス停の座標を使って距離から徒歩時間を出すこともできますが、
乗り継ぎ候補ごとにバス停を引き直す必要があり、リクエストが増えます。

---

## 地図から選ぶ

バス停は地図からも選べますが、**すでにわかっているバス停しか表示できません**。
ODPT のバス停には範囲検索が無く、全件取得も 1000 件の上限で頭打ちになるためです。
詳細と回避策は `MAP.md` を参照してください。
