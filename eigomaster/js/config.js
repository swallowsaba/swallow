/* ============================================================
   config.js — アプリ全体の共通設定（開発者が編集）
   ------------------------------------------------------------
   ★ 全ユーザー共通の「字幕中継サーバー」を使うには、
     下の RELAY_URL に、あなたがデプロイした中継のURLを1回だけ記入してください。
     例: "https://eigomaster-relay.あなたの名前.workers.dev/"
         "https://eigomaster-relay.deno.dev/"
   ・ここに入れた中継は【全ユーザー】が設定不要で自動的に使います。
   ・空 "" のままなら、各ユーザーの個別設定→公開サーバー→コピペ、の順で動きます。
   ・中継コードは「設定 → 動画字幕の自動取得 →（任意）中継の作り方」からコピーできます。
     同じコードが Cloudflare Workers / Deno Deploy のどちらでも動きます。
   ============================================================ */
window.EIGO_CONFIG = {
  // ↓↓↓ ここにデプロイ後のURLを貼るだけ（末尾の /?url= は付けても付けなくてもOK） ↓↓↓
  RELAY_URL: "firm-wolf-2949.swallowsaba.deno.net"
};
