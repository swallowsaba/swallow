# 地図から駅・バス停を選ぶ

`transit/js/map.js` / `transit/index.html` の「地図」カード

---

## できること

| 操作 | 結果 |
|---|---|
| 「地図を開く」 | 対応路線の**全駅**が緑の丸で出る |
| 駅をタップ | 「出発に設定」「到着に設定」「経由に追加」「この駅のバス停を表示」 |
| 「この駅のバス停を表示」 | その駅名で見つかったバス停を青い丸で追加 |
| バス停をタップ | 「出発に設定」「到着に設定」「経由に追加」 |
| 拡大する(GTFS の事業者) | 表示範囲のバス停がそのまま出る |
| **何も無い場所をタップ** | **その地点そのもの**を出発地・到着地・経由地にできる(最寄駅までの徒歩を計算) |
| **検索する** | いちばん上の経路の**乗降地点に番号付きの印**が自動で出る(地図が開いているとき) |
| 経路カードの「地図で見る」 | 示す経路を他の案に切り替える。表示中のカードには「地図に表示中」と出る |

出発地・到着地を地図から入れた場合、入力欄の下に「地図から選択」と出ます。

---

## 任意の地点を指定する

地図の**駅でもバス停でもない場所**をタップすると、そこを地点として使えます。
ポップアップに最寄駅と距離が出るので、確認してから「出発/到着/経由」を選びます。

指定した地点からは、**最寄駅まで歩く時間を見積もって**経路に組み込みます。

```
09:12  地点 35.6590, 139.7030
       [徒歩] 徒歩 約3分(渋谷まで)
       直線 約170m から算出  [推定]
09:15  渋谷
       [電車] 銀座線 …
```

### 徒歩時間の求め方 — 必ず読んでください

**徒歩の経路検索はしていません。** 歩道のデータを持っていないためです。
実際に計算しているのはこれだけです。

```
直線距離 × 1.3(迂回係数) ÷ 80m/分 = 徒歩の分数(切り上げ)
```

したがって、**川・線路・高速道路・大きな建物で回り道になる場所では実際より短く出ます。**
画面には必ず「推定」と根拠の直線距離を併記しています。鵜呑みにしないでください。

無料で規約に沿って使える徒歩ルーティング API が無いため、これ以上の精度は出せません。
値は `transit/data/config.json` で変えられます。

| 設定 | 既定 | 意味 |
|---|---|---|
| `walkSpeedMetersPerMinute` | 80 | 分速(m) |
| `walkDetourFactor` | 1.3 | 直線距離に対する実際の道のりの比 |
| `walkMaxCandidates` | 2 | 1 地点あたりに検討する駅の数 |
| `walkFarWarningMinutes` | 30 | これを超える徒歩に注意書きを添える(**検索は止めない**) |
| `walkMaxKm` | (なし) | 既定では**距離の上限はありません**。数値を入れたときだけ、それより遠い駅を候補から外します |

### 通信量に注意

地点を指定すると、**最寄駅の候補ごとに検索する**ため通信量が増えます。
出発・到着の両方を地点にすると候補の掛け算になるので、
組み合わせは **3 通りまで**に制限しています(徒歩が短い順に試します)。

### 距離では打ち切りません

**徒歩の距離に上限は設けていません。** 最寄駅まで徒歩 50 分でも、そのまま計算して出します。
30 分を超える場合だけ「最寄駅まで徒歩 約◯分あります」と添えますが、
それは注意書きであって、検索を止めるものではありません。

検索できないのは、**対応範囲に駅が 1 つも無いとき**だけです。

距離で切りたい場合は `transit/data/config.json` に `walkMaxKm` を足してください
(既定では設定していません)。

---

## 経路を地図に示す

検索すると、**いちばん上の経路の乗り降りする地点に番号付きの印**が出ます
(地図を開いているとき)。他の案は経路カードの「地図で見る」で切り替わります。
今示している経路のカードには「地図に表示中」と出て、枠が緑になります。

印は乗り物ごとに色を変えています。

| | 印 |
|---|---|
| 乗降する駅 | 緑の丸(路線色がデータにあればその色) |
| 乗降するバス停 | 青の丸 |
| 地図で指定した地点 | 茶の丸 |

出発地と到着地の印は大きく、途中の乗換地点は少し小さくしています。
通過するだけの駅には印を出しません。

### 線は引きません

以前は駅どうしを直線でつないでいましたが、**やめました。**

線路やバス路線の実際の形(線形)のデータを持っていないため、
直線でつないだ線は実際の走行経路とはまったく違うものになります。
それを地図に描くと、見た人に誤った経路を信じさせるだけで、何の役にも立ちません。

出すのは「**どこで乗って、どこで降りるか**」の位置だけにしています。

> 実際の線形を描くには、国土数値情報の鉄道データなどを別途取り込む必要があります。
> ファイルをリポジトリに同梱することになるため、今は入れていません。

---

## 使っている地図

| | |
|---|---|
| ライブラリ | Leaflet 1.9.4(cdnjs から読み込み) |
| タイル | [地理院タイル](https://maps.gsi.go.jp/development/ichiran.html) の淡色地図 |
| 条件 | **出典の表示とリンクのみ**。事前申請・API キーは不要 |
| 費用 | 無料 |

出典表示は地図の右下に常時出しています。消さないでください。

Google Maps / Mapbox のような従量課金・キー必須のものは使っていません。
Worker も経由しません(タイルは CORS 不要のただの画像のため)。

---

## 制約(重要)

### バス停は事業者によって出せる/出せないが分かれる

**GTFS を取り込んだ事業者(京王バス・小田急バス・西東京バス・関東バス)は、
拡大すれば表示範囲のバス停がそのまま出ます。**
索引に全停留所の座標を持っているためです。

| 状況 | 表示 |
|---|---|
| ズーム 13 未満 | 出さない(点で埋まって読めなくなるため。理由を画面に出す) |
| 範囲内が 400 件以下 | **全部出す**(「すべて表示しています(87 件)」) |
| 範囲内が 400 件超 | 各社から**均等に** 400 件(「1,832 件のうち 400 件だけ表示しています」) |

上限に達したときに先頭の事業者だけで枠を使い切ると、後ろの事業者の停留所が
1 件も出なくなります。そうならないよう、事業者から 1 件ずつ順に取っています。
**何件中何件を出しているのかも必ず画面に書きます**(全部出ていると誤解させないため)。

**それ以外の事業者(都営バスなど)は、今わかっているものしか出せません。**

駅は全件の座標を持っているので全部プロットできます。ODPT の API のバス停はできません。

ODPT のバス停(`odpt:BusstopPole`)には

- 範囲(緯度経度の矩形・半径)で検索する仕組みが無い
- 全件取得は 1000 件の上限があり、ページ送りも無い

ため、「地図に映っている範囲のバス停を出す」ということが原理的にできません。

そこで、次のいずれかで**すでに判明しているバス停だけ**を地図に出しています。

1. 名前で検索して見つかったバス停(入力欄に打った場合)
2. 表示中の経路に含まれるバス停
3. 駅をタップして「この駅のバス停を表示」を押したとき、その駅名で見つかったもの

つまり **ODPT の API を使う事業者については**、地図を開いてもバス停はほぼ出ません。
その場合は駅から辿るか、バス停名を入力欄に打つのが確実です。

**GTFS を取り込んだ事業者にはこの制限はありません。** 拡大すればそのまま出ます。
出ない場合は、まだ取り込みを実行していない可能性があります(`GTFS.md` を参照)。

### 座標が無いバス停は出せない

系統データ(`odpt:BusroutePattern`)からは停留所の**名前しか取れない**区間があります。
経路を地図に描くとき、座標が判っている停留所だけを点にしています。
そのため、バスの経路の線は途中が省略された直線になることがあります。

### 線は「駅を結んだ直線」

線路の形(実際の線形)のデータは ODPT の一般ライセンスでは扱えないため、
経路の線は駅とバス停を順に直線でつないだものです。**実際の走行経路とは違います。**

### Leaflet が読めないとき

CDN がブロックされている等で Leaflet を読み込めない場合、
地図だけを無効化して画面に理由を表示します。検索・経路探索はそのまま動きます。

---

## 検証

```bash
node tests/map.test.mjs      # 8 件:距離計算・経路 → 点列の変換
node tests/ui.smoke.mjs      # 画面の動作(地図の開閉・マーカー・選択・経路描画)
```

確認している内容:

- 鉄道の経路は**通過駅もすべて**点にする(逆方向でも並びが正しい)
- バスの経路は座標が判っている停留所だけを点にする
- 徒歩の乗り継ぎは点にしない
- 同じ座標が続いても重複させない
- 未知の路線が混ざっても落ちない
- 地理院タイルを読み込む設定になっている
- 駅のマーカーが生成される
- マーカーをタップ → 「出発に設定」で入力欄に入る
- 「地図で見る」で経路の線が描かれる
- Leaflet が無いときは理由を表示し、他の機能は動く

> スモークテストは、Leaflet を取得できない環境でもこちら側のコードを
> 検証できるよう最小のスタブを差し込んで実行しています。
> **地図の見た目そのものは、実際にブラウザで開いて確認してください。**
