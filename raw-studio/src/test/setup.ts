// Global test setup. Node 20+ already provides crypto.getRandomValues used by
// createId, so no polyfill is needed here yet. Add jsdom/testing-library setup
// when component tests are introduced.
export {};
