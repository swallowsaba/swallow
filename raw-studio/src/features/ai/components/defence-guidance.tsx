// This component was removed. The file is kept only as a harmless, unused
// placeholder so that overwriting the old defence-guidance.tsx fixes the build
// even if the file can't be deleted. It references no translation keys and is
// not imported anywhere. You can safely delete this file entirely.
export {};
